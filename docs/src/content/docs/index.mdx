---
title: React Sketch Canvas
description: A React component for freehand vector drawing with SVG.
template: splash
hero:
  tagline: Add a polished drawing surface to React apps without giving up SVG export, pointer-device control, or imperative canvas actions.
  image:
    file: ../../assets/logo.svg
  actions:
    - text: Get started
      link: /react-sketch-canvas/guides/installation/
      icon: right-arrow
      variant: primary
    - text: View on GitHub
      link: https://github.com/vinothpandian/react-sketch-canvas/
      icon: external
---

import { Card, CardGrid } from "@astrojs/starlight/components";
import { HomePageDemo } from "../../components/HomePageDemo";

React Sketch Canvas gives you the familiar feel of a drawing canvas while keeping every stroke as SVG path data. That makes it useful for signatures, annotations, teaching tools, whiteboards, and any UI where you need to save, replay, or export what someone drew.

The component stays intentionally small: you render a canvas, pass the drawing props you care about, and build the surrounding toolbar in React. The canvas handles pointer input and stroke history; your app decides how drawing fits into the product.

<HomePageDemo client:load />

## Why SVG?

Traditional `<canvas>` drawing is pixel-based. That is useful for painting apps, but it can be limiting when you need to store strokes, scale drawings, or export clean results later.

React Sketch Canvas records each stroke as path data. You can load those paths back into the canvas, export them as SVG, or render the same drawing at different sizes without the blurry edges that come from bitmap scaling.

## Features

<CardGrid>
  <Card title="Multi input support" icon="pencil">
    Accept mouse, touch, pen, Apple Pencil, Wacom, Surface Pen, and other pointer devices. You can also restrict drawing to one input type when your product needs tighter control.
  </Card>
  <Card title="Vector-first drawing" icon="add-document">
    Capture freehand strokes as paths, then export the result as SVG, PNG, or JPEG when you need to store or share the drawing.
  </Card>
  <Card title="Build your own toolbar" icon="pencil">
    Use refs for undo, redo, erase mode, reset, export, and sketching time. The library provides the drawing surface without forcing a specific UI.
  </Card>
  <Card title="Works with real app flows" icon="add-document">
    Load existing paths, render read-only drawings, place images behind the canvas, or collect timing data for review and analytics workflows.
  </Card>
</CardGrid>

## What can you build?

- Signature capture for forms, approvals, and consent flows.
- Image annotation tools for screenshots, photos, documents, and maps.
- Sketch pads for education, collaboration, and lightweight whiteboarding.
- Review screens where a saved drawing should be visible but not editable.
- Drawing inputs that must prefer a stylus, ignore touch, or support every pointer device.

## Explore examples

Start with the smallest useful setup, then move toward the parts most production drawing surfaces need: custom controls, input restrictions, undo history, image backgrounds, and timing.

<CardGrid>
  <Card title="Installation" icon="add-document">
    Install the package and render your first canvas. <a href="/react-sketch-canvas/guides/installation/">Start here</a>.
  </Card>
  <Card title="Drawing & Erasing" icon="pencil">
    Build a toolbar for draw mode, erase mode, colors, and read-only rendering. <a href="/react-sketch-canvas/examples/drawing/">Open example</a>.
  </Card>
  <Card title="Undo, Redo, Clear, and Reset" icon="add-document">
    Wire imperative canvas actions from your own controls. <a href="/react-sketch-canvas/examples/undo-redo/">Open example</a>.
  </Card>
  <Card title="Restrict Input" icon="pencil">
    Choose whether mouse, touch, pen, or every pointer type can draw. <a href="/react-sketch-canvas/examples/input-devices/">Open example</a>.
  </Card>
  <Card title="Background" icon="add-document">
    Place an image behind the drawing layer for annotation workflows. <a href="/react-sketch-canvas/examples/background/">Open example</a>.
  </Card>
  <Card title="Sketching Time" icon="pencil">
    Track active drawing time with timestamped strokes. <a href="/react-sketch-canvas/examples/sketching-time/">Open example</a>.
  </Card>
</CardGrid>
