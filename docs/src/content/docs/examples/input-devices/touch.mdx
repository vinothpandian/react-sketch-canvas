---
title: Only allow touch input
description: Accept touch input while ignoring mouse and pen input.
---

import ExampleBlock from "../../../../components/ExampleBlock";
import OnlyTouch from "../../../../examples/OnlyTouch";
import OnlyTouchSource from "../../../../examples/OnlyTouch.tsx?raw";

Use `touch` when the canvas is intended for finger input on touch screens. This keeps the interaction focused on mobile and kiosk-style experiences.

<ExampleBlock source={OnlyTouchSource}>
  <OnlyTouch />
</ExampleBlock>
