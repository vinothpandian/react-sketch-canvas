---
title: Background
description: Setting up background of the canvas.
sidebar:
  order: 5
---
import ExampleBlock from "../../../components/ExampleBlock.astro";
import Background from "../../../examples/Background";
import BackgroundSource from "../../../examples/Background.tsx?raw";
import BackgroundDataURI from "../../../examples/BackgroundDataURI";
import BackgroundDataURISource from "../../../examples/BackgroundDataURI.tsx?raw";

Background images are useful when the drawing is an annotation layer: mark up a photo, trace over a template, or collect notes on top of an existing asset.

Use `backgroundImage` with `preserveBackgroundImageAspectRatio` to draw over an image while controlling how the image fits the SVG canvas. The background stays separate from the user's strokes, so you can preserve the source image and export the combined result when needed.

<ExampleBlock source={BackgroundSource} previewClassName="min-h-96">
  <Background client:load />
</ExampleBlock>

## Data URI as Background

The same prop also accepts a data URI when the image is already available in memory. That is handy when users upload a file, when an API returns image data directly, or when you want to avoid managing a separate public URL.

The canvas API stays the same in both cases. Only the shape of the `backgroundImage` value changes.

<ExampleBlock source={BackgroundDataURISource} previewClassName="min-h-96">
  <BackgroundDataURI client:load />
</ExampleBlock>
